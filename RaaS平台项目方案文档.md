# 毕业设计项目方案文档

## 基于Spring Cloud的商用服务机器人集群运营调度平台（RaaS）的设计与实现

***

## 一、项目概述

### 1.1 项目名称

**基于Spring Cloud的商用服务机器人集群运营调度平台（RaaS）的设计与实现**

### 1.2 项目背景（!|>_<|!）

随着具身智能产业的快速发展（宇树、智元、普渡等厂商加速落地），商用服务机器人（送餐机器人、清洁机器人、巡检机器人）在商场、酒店、写字楼场景的渗透率快速提升。行业主流商业模式已从"卖机器人"转向 **RaaS（Robot as a Service，机器人即服务）**：商户不购买设备，按次/按时长购买机器人服务；设备由资产方投放运营，平台方负责撮合调度与分账结算。

当前中小型机器人运营方普遍存在以下痛点：

1. **调度低效**：任务分配依赖人工或简单的就近原则，未综合考虑电量、负载、设备健康度等因素；
2. **运营黑箱**：设备利用率、故障率、收益分账缺乏统一的数据视图，资产方无法精细运营；
3. **运维滞后**：设备故障依赖人工巡检发现，缺乏自动告警与工单闭环；
4. **系统割裂**：调度、计费、运维、监控各自独立，缺乏一体化的微服务平台支撑。

### 1.3 项目目标

构建一个基于 Spring Cloud 微服务架构的机器人集群运营调度平台，实现：

1. **设备统一接入**：基于 MQTT 协议接入机器人集群（含自研模拟器），实时感知设备状态与位置；
2. **智能任务调度**：实现"硬约束筛选 + 多目标加权评分 + 原子锁定"的三段式调度引擎，支持贪心/加权/匈牙利算法三种策略的对比实验；
3. **完整商业闭环**：商户下单 → 平台调度 → 设备执行 → 自动计费 → 三段式分账；
4. **运维保障体系**：故障自动告警、工单流转、设备健康度评分，健康度反馈至调度权重形成闭环；
5. **多端触达**：uni-app 一套代码编译微信小程序 + Android APP，四类角色均有专属工作台；
6. **实时可视化**：WebSocket 驱动的数字孪生监控大屏，机器人位置与状态实时呈现。

### 1.4 项目定位说明

- **自研核心**：调度引擎、机器人模拟器、订单状态机、分账模型、监控推送（预估自研代码 3000\~5000 行）；

- **复用组件**：Spring Cloud 全家桶、EMQX、RabbitMQ、PostgreSQL/PostGIS、匈牙利算法开源实现（工程选型，非论文抄袭范畴）；

- **验证方式**：使用遵循真实 MQTT 协议的自研模拟器集群验证系统，协议层兼容真实设备接入。

***

## 二、需求分析

### 2.1 角色定义（4 类角色）(APP 暂定为小程序)

| 角色       | 定位                   | 核心职责                            | 使用端          |
| -------- | -------------------- | ------------------------------- | ------------ |
| ① 平台运营方  | 撮合方/裁判（中间人）          | 商户与资产方准入审核、分账规则配置、全局监控大屏、违规处置   | Web 大屏 + 小程序 |
| ② 机器人资产方 | 设备的归属方（机器人的所有权）      | 注册/绑定机器人、投放场站、设定计价策略、查看收益与利用率报告 | APP + 小程序    |
| ③ 商户     | 服务的租赁方（机器人和平台服务的使用权） | 扫码/小程序下单、任务实时跟踪（ETA）、账单支付、评价    | 小程序          |
| ④ 运维工程师  | 设备的医生（后期）         | 接收故障告警、工单接单、维修记录填报、设备健康度查看、巡检打卡 | APP          |

**角色关系**：商户下单买服务 → 平台调度 → 资产方设备执行 → 故障时运维维修 → 平台运营方监管分账。一笔订单的生命周期串起全部四角色。

### 2.2 功能需求

#### 2.2.1 设备接入模块

- 机器人通过 MQTT 接入（主题：`robot/{id}/telemetry` 上行遥测、`robot/{id}/command` 下行指令）；

- 遥测数据含：坐标、电量、状态、当前任务 ID、故障码，1 秒/次上报；

- 心跳哨兵：超 10 秒无心跳自动转"离线"态并触发重调度；

- 设备状态机：空闲 → 执行中 → 返充中 → 故障 → 离线。

#### 2.2.2 调度引擎模块（核心）

- 三段式调度流程：硬约束筛选 → 多目标打分 → Redis+Lua 原子锁定指派；

- 硬约束：状态=空闲、电量 > 预估消耗 × 1.5、同楼层、机型匹配任务类型；

- 打分公式：`score = w1·距离分 + w2·电量分 + w3·负载均衡分`，权重支持 Nacos 热更新；

- 三种调度策略可切换：贪心（最近优先）/ 加权评分 / 匈牙利算法（批量全局最优）；

- 异常重调度：执行中失联/电量骤降/故障码上报 → 任务自动回炉换机重派。

#### 2.2.3 任务订单模块

- 任务类型：送餐到桌 / 区域清洁 / 定时巡检；

- 订单状态机：待调度 → 已指派 → 执行中 → 已到达 → 待确认 → 已完成 / 异常重调度 / 已取消；

- 商户端实时跟踪：机器人位置、ETA 倒计时（WebSocket 推送）；

- 到达超时自动确认（防"占着设备不放"）。

#### 2.2.4 计费分账模块

- 计价模式：按次计费 / 按时长计费（跨整点分段）/ 包月套餐；

- 三段式分账流水：商户付款 → 平台抽佣 X% → 资产方分成；

- 月度账单、分账明细报表。

#### 2.2.5 运维工单模块

- 故障码上报自动生成工单；

- 工单流转：待接单 → 已接单 → 维修中 → 已完成 → 已复核；

- 维修记录沉淀 → 设备健康度评分模型（维修历史 + 故障频率加权）；

- **健康度反哺调度**：评分低的设备被调度引擎降权。

#### 2.2.6 监控推送模块

- 数字孪生大屏：场站平面图 + 机器人实时位置动画 + 订单量/异常指标；

- WebSocket 实时推送（位置、状态变更、告警）；

- 告警触达：小程序订阅消息 / APP 推送。

#### 2.2.7 用户与权限模块

- RBAC 权限模型：role → permission → menu/api/resources 三级映射；

- 数据行级隔离：资产方仅可见自己名下设备数据，商户仅可见自己订单与账单；

- 一个用户多角色支持（角色切换，非多账号）。

### 2.3 非功能需求

| 指标       | 目标值                   |
| -------- | --------------------- |
| 调度决策耗时   | 单任务 < 100ms           |
| 模拟集群规模   | 支持 50 台机器人并发在线        |
| 遥测消息处理   | 50 msg/s 稳定消费，MQ 削峰落库 |
| 大屏位置推送延迟 | < 1s                  |
| 配额/锁定一致性 | 并发派单零冲突（Lua 原子保证）     |

***

## 三、系统架构设计

### 3.1 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│  客户端层（uni-app 一套代码 → 微信小程序 + Android APP）        │
│  ①运营大屏/审批   ②资产方看板   ③商户下单   ④运维接单          │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS / WebSocket / JWT
┌──────────────────────────▼──────────────────────────────────┐
│              Spring Cloud Gateway（统一网关）                 │
│           路由转发 · JWT鉴权 · 接口限流 · 日志埋点              │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      微服务层（6 个服务）                      │
│                                                             │
│  设备接入服务   调度引擎服务   任务订单服务                      │
│  计费分账服务   运维工单服务   监控推送服务                      │
│                                                             │
│   Nacos 注册/配置中心 ｜ OpenFeign 服务调用 ｜ Sentinel 熔断    │
└──────┬───────────────────────────────────────────┬──────────┘
       │                                           │
┌──────▼──────────┐                    ┌───────────▼──────────┐
│   设备侧通道      │                    │      基础设施层        │
│  EMQX (MQTT)    │                    │ PostgreSQL + PostGIS │
│  机器人模拟器 ×N  │                    │ Redis ｜ RabbitMQ    │
└─────────────────┘                    │ Docker Compose       │
                                       └──────────────────────┘
```

### 3.2 微服务划分

| 服务名                        | 职责                                    | 核心技术点                     |
| -------------------------- | ------------------------------------- | ------------------------- |
| **gateway**                | 统一 API 入口、JWT 鉴权、限流、日志埋点              | Spring Cloud Gateway 过滤器链 |
| **device-service** 设备接入    | MQTT 消息收发、遥测解析、Redis 状态快照、心跳哨兵、历史轨迹落库 | EMQX 客户端、MQ 削峰批量写入        |
| **dispatch-service** 调度引擎  | 三段式调度、三种算法策略、原子锁定、异常重调度               | Redis+Lua、PostGIS KNN     |
| **order-service** 任务订单     | 下单、订单状态机、执行跟踪、ETA 计算                  | 状态机、WebSocket 联动          |
| **billing-service** 计费分账   | 计价引擎、分账流水、账单报表                        | 金额精度、窗口函数                 |
| **workorder-service** 运维工单 | 工单流转、健康度评分、巡检管理                       | 评分模型                      |
| **monitor-service** 监控推送   | 大屏数据源、WebSocket 推送、告警分发               | WebSocket 集群、订阅消息         |
| **user-service** 用户权限      | RBAC、行级数据隔离、角色切换                      | Spring Security           |

> 注：gateway 与 user-service 为基础支撑服务，与 6 大业务服务合计 8 个模块。

### 3.3 技术选型

| 层次          | 选型                                                | 版本建议         |
| ----------- | ------------------------------------------------- | ------------ |
| 开发语言        | Java                                              | 21           |
| 微服务框架       | Spring Boot + Spring Cloud + Spring Cloud Alibaba | 4.0.8 / 2025.1.0 / 2025.1.0.0 |
| 注册/配置中心     | Nacos                                             | laster（跟随最新版）         |
| 网关          | Spring Cloud Gateway                              | 随 SC 版本      |
| 服务调用        | OpenFeign + LoadBalancer                          | 随 SC 版本      |
| 熔断限流        | Sentinel                                          | 待定      |
| 数据库         | PostgreSQL + PostGIS 扩展                           | 16           |
| ORM         | MyBatis-Plus（主键策略 ASSIGN\_ID 雪花算法）                | 待定         |
| 缓存          | 默认 derby 存储（暂时）                                             | Nocas 默认          |
| 消息队列        | RabbitMQ                                          | 待定        |
| MQTT Broker | EMQX                                              | 待定          |
| 前端          | uni-app（Vue3 语法）                                  | HBuilderX 最新 |
| 部署          | Docker + Docker Compose                           | —            |

**PostgreSQL 选型理由**（论文技术选型论证素材）：

1. PostGIS 空间扩展支持 KNN 最近邻查询（`ORDER BY pos <-> point LIMIT 5`），是就近调度的数据库层地基，MySQL 空间能力远弱于此；
2. 窗口函数支撑利用率/故障率时序分析；
3. JSONB 存储异构设备参数（不同型号机器人字段差异）；
4. 国产数据库（openGauss 等）基于 PG 内核，技术栈契合国产化趋势。

### 3.4 数据流设计

**实时链路**（要求低延迟）：

```
机器人遥测 → EMQX → device-service → Redis 状态快照（调度决策读这里）
                                     → monitor-service → WebSocket → 大屏/小程序
```

**异步链路**（要求可靠）：

```
遥测历史 → RabbitMQ → 批量落库 PostgreSQL（仅用于回放与分析）
订单完成事件 → RabbitMQ → billing-service 计费分账
故障码事件 → RabbitMQ → workorder-service 生成工单 + 告警推送
```

***

## 四、核心技术方案

### 4.1 MQTT 协议设计（模拟器与接入服务的契约）

| 主题                     | 方向 | Payload 要点                                       |
| ---------------------- | -- | ------------------------------------------------ |
| `robot/{id}/telemetry` | 上行 | `{x, y, battery, status, taskId, errorCode, ts}` |
| `robot/{id}/command`   | 下行 | `{action: "DISPATCH", taskId, target: {x, y}}`   |
| `robot/{id}/ack`       | 上行 | `{taskId, accepted: true/false}`                 |
| `robot/{id}/event`     | 上行 | `{event: "ARRIVED"/"COMPLETED"/"FAULT", detail}` |

**机器人模拟器设计**（自研，Java 进程，约 200 行）：

- 启动参数：机器人 ID、初始坐标、速度、电量消耗速率；

- 循环逻辑：接收指令 → 沿坐标线性移动（每秒一跳）→ 定时上报遥测 → 到达后上报事件；

- 支持故障注入（模拟掉线、电量骤降、随机故障码）——为实验与演示提供可控变量。

### 4.2 调度引擎设计（权重平调）

```
调度触发（三种来源）
├── 新订单入队（MQ 消费）
├── 异常重调度（失联/故障/低电量）
└── 定时任务（凌晨巡检批量生成）

调度流程
Step1 硬约束筛选
      状态=空闲 AND 电量>预估消耗×1.5 AND 同楼层 AND 机型匹配
      → 候选池（空则订单进入等待队列）
Step2 多目标打分
      score = w1·(1/距离) + w2·(电量/100) + w3·负载均衡因子
      （w1/w2/w3 存于 Nacos 配置，支持热更新）
Step3 原子锁定
      Redis Lua 脚本：CAS 将机器人状态 空闲→已锁定
      成功 → 下发 MQTT 指令 + 订单状态推进
      失败（并发抢同机）→ 取下一候选
```

**三种策略实现**：（目前代码编写阶段不涉及；9月10日）

1. **贪心**：每单独立选评分最高者（约 10 行核心代码）（最广算法）；
2. **加权评分**：主方案，权重敏感性实验（约 30 行）；
3. **匈牙利算法**：攒批订单构建二分图代价矩阵，求全局最优指派（复用开源实现，工程化集成）。

### 4.3 实验设计（论文实验章节）（暂定测试）

| 实验         | 方法                               | 指标                                |
| ---------- | -------------------------------- | --------------------------------- |
| **调度算法对比** | 模拟器批量发单（100/500/1000 单），三种策略各跑多轮 | 平均任务完成时间、总行驶距离、利用率方差、电量消耗         |
| **并发压力**   | 50 台模拟器 + 持续发单                   | 调度吞吐、MQTT 消息延迟、WebSocket 推送延迟、P99 |
| **故障恢复**   | 随机 kill 机器人进程                    | 重调度成功率、恢复时间                       |
| **权重敏感性**  | 变动 w1/w2/w3 观察指标变化               | 各指标对权重的响应曲线                       |

***

## 五、数据库设计（核心表概要）（暂定）

| 表                                          | 关键字段                                                  | 说明           |
| ------------------------------------------ | ----------------------------------------------------- | ------------ |
| `sys_user` / `sys_role` / `sys_permission` | RBAC 标准三表 + user\_role 关联                             | 权限模型         |
| `asset_owner` / `merchant`                 | 关联 user\_id，资质字段                                      | 资产方/商户档案     |
| `station` 场站                               | `pos geography(Point)`，楼层，平面图 URL                     | PostGIS 空间字段 |
| `robot` 机器人                                | 型号、能力集（JSONB）、`pos geography(Point)`、状态、健康度、owner\_id | 异构参数 JSONB   |
| `task_order` 订单                            | 商户、任务类型、起终点、状态、ETA、评分                                 | 状态机流转字段      |
| `dispatch_record` 调度流水                     | 订单 ID、机器人 ID、策略、评分快照、结果                               | 实验数据来源       |
| `telemetry_log` 遥测历史                       | 机器人 ID、坐标、电量、时间（按时间分区）                                | 回放与分析        |
| `billing_record` 计费流水                      | 订单 ID、金额、计价模式、分段明细（JSONB）                             | NUMERIC 精度   |
| `split_rule` / `split_record`              | 分账规则与流水（商户→平台→资产方）                                    | 三段式          |
| `work_order` 工单                            | 机器人 ID、故障码、状态、维修记录、耗时                                 | 工单流转         |
| `health_score` 健康度                         | 机器人 ID、评分、因子明细、更新时间                                   | 反哺调度权重       |

***

## 六、进度安排（2026.09 — 2027.05）（暂定，弹性调节）

| 阶段     | 时间                | 内容                                                       | 里程碑         |
| ------ | ----------------- | -------------------------------------------------------- | ----------- |
| 调研与开题  | 2026.09 — 2026.11 | 文献调研、需求分析、开题报告撰写与答辩                                      | 开题答辩（11 月初） |
| 总体设计   | 2026.11 — 2026.12 | 架构设计、数据库设计、MQTT 协议定稿、UI 原型                               | —           |
| 基础设施搭建 | 2026.12 — 2027.01 | Docker Compose 编排（Nacos/PG/Redis/MQ/EMQX）、模拟器开发、设备接入服务跑通 | 模拟器遥测上屏     |
| 核心开发一  | 2027.01 — 2027.02 | 调度引擎（先贪心跑通全链路）、订单服务                                      | 全链路首次打通     |
| 核心开发二  | 2027.02 — 2027.03 | 计费分账、运维工单、监控推送、用户权限                                      | 中期检查（3 月中）  |
| 双端开发   | 2027.03 — 2027.04 | uni-app 四角色工作台（小程序 + APP 双端）                             | 双端联调完成      |
| 实验与测试  | 2027.04           | 三种调度算法对比实验、并发压测、故障恢复测试                                   | 实验数据齐备      |
| 论文与答辩  | 2027.04 — 2027.05 | 论文撰写、查重降重、答辩演练                                           | 毕业答辩（5 月底）  |

***

## 七、预期成果

1. **可运行系统**：Docker Compose 一键部署全套微服务 + 模拟器集群 + 四角色双端；
2. **实验报告**：调度算法对比、并发压测、故障恢复的完整量化数据与图表；
3. **毕业论文**一篇（含架构设计、调度算法、实验分析核心章节）；
4. **演示视频**：四角色全链路演示（下单→调度→执行→故障→工单→分账）。

***

*文档版本：v1.0*
